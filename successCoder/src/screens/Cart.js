import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Cart = ({ cart }) => {
  return (
    <View>
      <Text>{cart}</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Cart;
